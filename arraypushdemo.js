var firstArr = [];
var lastArr = [];

var userFirst= prompt("First name");
firstArr.push(userFirst);

var userLast = ("Last name");
lastArr.push(userLast);

window.alert(userFirst[0] + " " + userLast[0] + firstArr.length + lastArr.length);
